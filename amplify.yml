version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp *.html dist/
        - cp robots.txt sitemap.xml dist/
        - cp -r css js assets pieces dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
